/**
 * PostCSS Configuration
 *
 * PostCSS processes CSS with plugins. Here we use:
 * - tailwindcss: Processes Tailwind utility classes
 * - autoprefixer: Adds vendor prefixes for browser compatibility
 */

module.exports = {
  plugins: {
    tailwindcss: {},
    autoprefixer: {},
  },
};
